# 🎯 DEMO分支 - 最小可运行版本

这是一个从零开始创建的完全独立的最小版本，专门用于演示和学习。

## ✨ 特性

- ✅ 完全干净的代码库（无历史包袱）
- ✅ 最小文件结构
- ✅ 独立运行端口（避免冲突）
- ✅ 清晰的架构展示

## 🚀 快速开始

### 后端启动
```bash
cd backend
npm install
npm start
# 访问: http://localhost:3001
```

### 前端启动
```bash
cd frontend
npm install
npm run dev
# 访问: http://localhost:5174
```

## 📁 项目结构

```
volunteer-demo-clean/
├── backend/           # 最小后端服务
│   ├── server.js     # 基础API服务
│   └── package.json  # 后端依赖
├── frontend/         # 最小前端应用
│   ├── src/          # React组件
│   ├── index.html    # HTML入口
│   ├── vite.config.js # 构建配置
│   └── package.json  # 前端依赖
└── .gitignore        # Git忽略配置
```

